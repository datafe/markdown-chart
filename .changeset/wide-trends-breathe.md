---
'@datafe-open/markdown-chart-echarts': patch
---

Raise the default narrow-table ECharts budget to 100,000 rows, 1,000,000 cells,
and 1,200,000 JSON nodes, and allow legacy ChatBI CSV artifacts up to 64 MiB.
The 200-column and five-second legacy boundaries remain in place.
