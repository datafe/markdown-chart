---
"@datafe-open/markdown-chart-echarts": patch
---

Restore string cell semantics in the deprecated ChatBI legacy CSV adapter so saved chart scripts can continue using string operations on digit-only dates and other CSV values.
