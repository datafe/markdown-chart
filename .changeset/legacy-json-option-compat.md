---
"@datafe-open/markdown-chart-echarts": patch
---

Render strict JSON option bodies in temporary ChatBI query fences without artifact lookup or legacy JavaScript sandboxing, while keeping unsafe JSON fail-closed and legacy JavaScript fallback behavior unchanged.
