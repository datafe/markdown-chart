---
"@datafe-open/markdown-chart-echarts": patch
---

Add a host-transport-based legacy sandbox client with principal-isolated caching, deterministic request-to-session retries, unique CSV matching, stable public errors, and renderer bindings that preserve existing deprecated callback behavior.
